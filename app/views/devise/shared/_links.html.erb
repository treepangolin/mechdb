<ul class="nav">
  <%- if controller_name != 'sessions' %>
    <li class="nav-item">
      <%= link_to 'Sign in', new_session_path(resource_name), class: 'nav-link' %>
    </li>
  <% end %>

  <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
    <li class="nav-item">
      <%= link_to 'Create account', new_registration_path(resource_name), class: 'nav-link' %>
    </li>
  <% end %>

  <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
    <li class="nav-item">
      <%= link_to 'Forgot password', new_password_path(resource_name), class: 'nav-link' %>
    </li>
  <% end %>

  <%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
    <li class="nav-item">
      <%= link_to 'Can\'t confirm', new_confirmation_path(resource_name), class: 'nav-link' %>
    </li>
  <% end %>

  <%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
    <li class="nav-item">
      <%= link_to 'Can\'t unlock', new_unlock_path(resource_name), class: 'nav-link' %>
    </li>
  <% end %>
</ul>

<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <%= link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider) %>
  <% end %>
<% end %>
